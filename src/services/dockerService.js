// The Docker service implementation is shared between the desktop
// application-host execution target and the standalone Agent runtime.
// See src/shared/dockerService.js for the canonical implementation.
module.exports = require("../shared/dockerService");
