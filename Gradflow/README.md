# Gradflow

Gradflow is a Java 21 Gradle desktop starter project.

## Requirements

- Debian or another Linux distribution
- Java 21

The Gradle wrapper is included, so Gradle does not need to be installed globally.

## Commands

```bash
./gradlew --version
./gradlew build
./gradlew run
```
